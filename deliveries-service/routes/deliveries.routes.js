const express = require('express');
const router = express.Router();


// Controller functions
const deliveries_controller = require('../controllers/deliveries.controller.js')

module.exports = router;